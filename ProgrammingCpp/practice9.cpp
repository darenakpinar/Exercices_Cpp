#include <iostream>
#include <string>
#include <vector>

int count (std::vector<int>n &numbers, int n){

	if (n== 0) return 1;

	if (n<0)return 0;

	int total_count = 0;
	for (int t = count(numbers,n-numbers.at(i)){
		tot_count = tot_count + t;
	}
return tot_count;
}
