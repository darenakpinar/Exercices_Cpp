#include <iostream>
#include <vector>
using namespace std;
template <typename T>
class Stack{

private:
    vector<T> container;

public:

T (element) : element(elem){ }  
void push(const T& element){
    push_back(element);

}

void pop(){
    if (T == nullptr){
        cout << "Stack is empty!"<< endl;
        return;
    }
    pop_back();
}
T top() const{
    if (T == nullptr) return;

    return  
    
}
bool isEmpty(){
    if (T== nullptr)return false;

    else return true;
}
};

int main(){
    Stack<int> t;
    
    t.push(20);
    t.push(30);
    t.push(40);
    t.push(50);
    
    cout << "After push_back: " << 

}