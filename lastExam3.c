#include <stdio.h>
#include <string.h>

int csv_sum(const char *S){
    
    int len = strlen(S);
    int arr[len];
    for (int i = 0; i < len; i++){
        arr += atoi(const char *S)
    }

}

int main(){

    const char *S = "10,20,-5, 2";
    printf("%d\n", csv_sum(S)); // 27 because 10 + 20 - 5 + 2 = 27

    return 0;
}  