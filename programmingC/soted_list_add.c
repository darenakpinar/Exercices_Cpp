#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

typedef struct n{
    char letter;
    int freq;
    struct n* next;
}node;

void sorted_add(node **head, char l, int f){
    
}