#include <iostream>
#include <cmath>
#include <vector>
#include <assert>
using namespace std;
void  pushBack(vector<int>& numbers, int n, cout = 0){
	if (count == 0
	count = 0;
	numbers.push_back(x);
	if (numbers.size() == )return 1; // base case

	return pushBack(numbers, n, x+1);
}



int Count(vector<int>& numbers,int n){
}

int main(){

int n, a;


cin >> n;

cout << count(a, n) << endl;
return 0;


}
